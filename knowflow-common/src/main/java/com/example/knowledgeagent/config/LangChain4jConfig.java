package com.example.knowledgeagent.config;

import org.springframework.context.annotation.Configuration;

@Configuration
/**
 * 定义 LangChain4jConfig 组件，承载对应模块的业务职责。
 */
public class LangChain4jConfig {
    // LangChain4j dependency is kept in the project so the model layer can switch to its adapters later.
}
