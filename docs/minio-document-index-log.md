[2026-05-26 11:04:43] MinIO 直传后的文档索引链路优化
- 修改内容：拆分 document-service 文档索引长事务，解析、状态更新、embedding 批处理、切片/向量替换改为阶段化短事务；新增文档索引专用异步线程池和可配置 embedding batch；索引任务写入阶段重新读取最新文档约束，支持索引完成前调整资料层级；删除中的文档会让索引任务主动中止并标记 CANCELLED；统计同步保持 best-effort，避免派生统计影响上传、删除和索引主链路。
- 注意事项：本次保持现有高质量 embedding 链路，不主动缩短超时或提前降级；如果生产环境索引并发继续升高，可继续评估 MQ 化任务队列和更细的索引取消状态展示。
